# Generated by SDP Builder — Declarative Automation Bundle
# https://docs.databricks.com/aws/en/dev-tools/bundles/reference
bundle:
  name: new_pipeline_test
  uuid: 240cd270-9549-5568-b53c-a578b012bf2f

include:
  - resources/*.yml

variables:
  catalog:
    description: Unity Catalog for pipeline publish
    default: "classic_stable_kremer_catalog"
  schema:
    description: Schema for pipeline publish
    default: "silver_test"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: "https://fevm-classic-stable-kremer.cloud.databricks.com"
    presets:
      pipelines_development: true
      trigger_pause_status: PAUSED
      source_linked_deployment: true
  prod:
    mode: production
    workspace:
      host: "https://fevm-classic-stable-kremer.cloud.databricks.com"
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: "classic_stable_kremer_catalog"
      schema: "silver_test"
